# Shared A2A server — runtime deps (STARTER; backend-engineer owns evolution).
# Mirrors the handoff_mcp HTTP-server stack + what the contract binding implies:
# JSON-RPC/SSE over HTTP (starlette/uvicorn), pydantic models (contract client),
# OIDC bearer validation + JWS card signing.
uvicorn>=0.30
starlette>=0.37
pydantic>=2.7
httpx>=0.27
PyJWT>=2.8
cryptography>=42
PyYAML>=6
