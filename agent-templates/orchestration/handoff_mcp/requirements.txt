mcp>=1.2.0
uvicorn>=0.30
starlette>=0.37
# A2A transport: the FROZEN generated client (contracts/a2a/v1/client) speaks
# JSON-RPC + SSE via httpx and validates wire/card models via pydantic v2.
httpx>=0.27
pydantic>=2.6
