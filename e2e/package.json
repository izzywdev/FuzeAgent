{
  "name": "fuzeagent-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Post-production Playwright smoke tests — verify FuzeAgent loads inside FuzeFront",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^24.13.3"
  },
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.0.0"
  }
}
